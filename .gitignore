#
# Git の管理対象から外したい（無視したい）ファイルやフォルダを指定するための設定ファイル
#

# --- MATLAB 関連の生成ファイル ---
*.asv       # 編集中の自動保存ファイル (Auto-Save)
*.mex*      # コンパイル済みのバイナリファイル (プラットフォーム依存のため除外)
*.p         # コンパイル済みコンテンツ保護ファイル
*.slxc      # Simulink キャッシュファイル (高速化用の一時ファイル)
*.autosave  # Simulink 等の自動保存
*.log       # MATLAB 実行時のログファイル

# --- Simulink / 一時フォルダ ---
slprj/      # Simulink のシミュレーション実行時に生成される中間コードフォルダ
# (注: *.slxc は上記 MATLAB セクションと重複していますが、Simulink 用として明示)

# --- エディタ / OS 固有の一時ファイル ---
*.swp       # Vim 等のエディタが作成するスワップファイル
.DS_Store   # macOS のフォルダ設定ファイル
Thumbs.db   # Windows の画像キャッシュファイル

# --- プロジェクト固有のビルド成果物 ---
# buildfile.m や CI によって生成されるテストレポート等
build-output/